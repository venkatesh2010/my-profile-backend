# application.properties

# Application Name (this is fine)
spring.application.name=my-profile-site-backend

# Database Connection
# IMPORTANT: Ensure this URL is copied EXACTLY from Render's "External Connection String"
# It must start with 'jdbc:' and include the port number (e.g., :5432).
# AND include any SSL parameters like '?sslmode=require' if present in Render's string.
# The username and password are now provided in separate properties below.
spring.datasource.url=jdbc:postgresql://dpg-d0ta6r63jp1c73ecf4l0-a.oregon-postgres.render.com:5432/profiledb_5mhs?sslmode=require
spring.datasource.username=profileuser
spring.datasource.password=CyEAazh5k9qlFOENIWA5UedksiP10BxW

spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# CORS Configuration for Frontend
# This allows your frontend (running on localhost:3000) to make requests to your backend.
# Replace http://localhost:3000 with your frontend's deployed Vercel URL later.
app.cors.allowed-origins=http://localhost:3000
